# Repo Triage Plan — 2026-03-08

> Post-SitDeck campaign. Two repos need cleanup: auto-sdd (build system) and compstak-sitdeck (built app).

## compstak-sitdeck (the built app)

1. **Merge campaign branch into main.** Branch `auto/chained-20260308-193835` has 60 commits with 36 built features. Main is empty scaffolding without them.

2. **Delete orphan campaign branches.** ~20+ `auto/chained-*` branches from failed and successful runs. All redundant after merge.

## auto-sdd (the build system)

3. **Triage untracked/modified files in `git status`.** Categorize each group as commit, gitignore, or discard:
   - Build logs (`logs/compstak-sitdeck/build-*.log`) — commit or gitignore?
   - Eval results (`logs/compstak-sitdeck/evals/`) — same question.
   - Eval sidecar log + cost log (modified, large) — same question.
   - Validation run outputs (`logs/validation/val-*`) — same question.
   - Archived handoff (`archive/handoffs/handoff-2026-03-08.md`) — commit.
   - Modified specs in compstak-sitdeck (drift reconciliation artifacts) — tracked in nested repo, should be ignored by auto-sdd's git.
   - `learnings/pending.md`, `general-estimates.jsonl`, `.specs/learnings/general.md` — review contents, commit if meaningful.
   - Modified test file (`py/tests/test_overnight_autonomous.py`) — review diff, commit if intentional.

4. **Update `.gitignore` in auto-sdd** based on decisions from step 3. Logs and eval outputs probably shouldn't be tracked long-term.

5. **Commit the triage results** in auto-sdd.

6. **Delete orphan branches in auto-sdd.** `auto/chained-*` and `claude/*` branches — dozens of them, all stale.

## Verification

7. **Run auto-sdd test suite** to confirm nothing broke during campaign fixes.

8. **Confirm both repos have clean `git status`** after triage.

---

Steps 3-4 require Brian's input. The rest are mechanical.
