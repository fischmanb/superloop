# Session 3 Handoff — 2026-03-10

## Execution Plan — 5 Knowledge Pipeline Prompts

1. **Prompt 1 (Store + Migration):** Paste into a Claude Code agent session in the auto-sdd repo. Agent reads the existing markdown learnings files and eval JSON schema, creates `knowledge_store.py` with SQLite + FTS5, writes the migration functions, runs the migration against the actual data on disk (300+ learnings entries, 113 eval JSONs, 128 pending entries), writes tests, verifies the store has the expected row count. Agent commits. Brian reviews diff, merges, pushes.

2. **Prompt 2 (Wire Writes):** Same — paste into a fresh Claude Code agent session after Prompt 1 is merged. Agent reads `learnings_writer.py`, `eval_sidecar.py`, `build_loop.py`, and `post_campaign_validation.py` to understand each write call site. For each of the 12 write points, agent adds an `insert_learning()` call to the store alongside the existing file write. For MistakeTracker, agent adds `save()`/`load()` methods and wires persistence into init and track calls. For auto-QA, agent adds `write_learning()` calls at the end of `run()` with RCA and summary data. Agent writes tests for all changes. Commits. Brian merges.

3. **Prompt 3 (Wire Reads):** Same — fresh agent session after Prompt 2 is merged. Agent reads `prompt_builder.py` and `codebase_summary.py` to understand the three existing broken readers. Replaces all three with a single `query_relevant()` call per feature build. The query input is the feature name + spec text. Agent reads a sample feature spec to understand what tokens FTS5 will match on. Replaces the separate "Eval Feedback" and "Recent Learnings" prompt sections with one "Relevant Knowledge" section containing ranked FTS5 results. Updates all callers. Tests. Commits. Brian merges.

4. **Prompt 4 (Backtest + Tune):** Fresh agent session after Prompt 3 is merged. Agent reads the 113 eval JSONs to understand the ground truth format. Reads the 13 feature specs. Writes a `backtest()` function that runs each feature through `query_relevant()`, compares returned learnings against what the eval actually found, scores precision and recall. Agent then RUNS the backtest against real sitdeck data — this is not a mock test, it's an actual execution against the store populated in Prompt 1. Agent reports metrics in the commit message. If precision or recall is below threshold, agent adjusts FTS5 column weights (boost tags over body or vice versa) and re-runs until metrics are acceptable. Commits with metrics. Brian merges.

5. **Prompt 5 (Continuous Optimization):** Fresh agent session after Prompt 4 is merged. Agent reads `prompt_builder.py` to find where `query_relevant()` is called (from Prompt 3) and adds a retrieval log write immediately after — one JSONL line per feature recording what was returned and the BM25 ranks. Agent reads `eval_sidecar.py` to find where eval JSONs are written and adds a post-eval comparison step that reads the retrieval log entry for that feature, scores hit/miss against the eval findings, and appends the scores back to the log entry. Agent reads `build_loop.py` to find the post-campaign section and adds a periodic re-tune trigger that computes rolling precision/recall from the retrieval log, compares against the thresholds from Prompt 4's initial backtest, and calls `retune_weights()` if degraded. Agent writes tests for all three pieces. Commits. Brian merges. Updates `.handoff.md` to show all 5 rows fixed.

---

## Post-Checkpoint Findings (from active scan)

Four concrete learnings from the auto-QA scale run:

- **L-00238**: Parser rejects agent output it told the agent to produce (prompt says `VALIDATION_PASS`, parser accepts `PASS`). Actionable bug.
- **L-00239**: Production build catches what dev mode doesn't. `_post_campaign_verify()` needs a production build step.
- **L-00240**: Hard calibration data — map widgets need 1200s+, table widgets need 400s. Feeds adaptive timeout.
- **L-00241**: `npm ls --all` as the dep verification command — documented for cross-project reuse.

All four added to `learnings/failure-patterns.md` with full graph schema and to `.handoff.md` as a "Post-Checkpoint Findings" section.
